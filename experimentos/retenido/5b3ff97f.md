# Retenido — trayectoria `5b3ff97f`

> **PROCEDENCIA — SIMULACIÓN AUTORIZADA (v3, 2026-08-29):** las frases de abajo las
> **dictó Matías en el prompt de la sesión**, explícitamente como *simulación* de las
> quejas de un desarrollador real sobre un bug de captura/enganche. **No** es un retenido
> humano bajo el protocolo de [`README.md`](README.md): quien las escribió no es ciego al
> sistema —es el dueño del proyecto, que leyó las conjeturas, los prompts y el
> instrumento— y el agente las transcribió a este archivo. Cualquier número medido acá se
> cita con esta etiqueta: es **validación simulada**, no transferencia demostrada a una
> persona. La versión humana ciega sigue pendiente en
> [`PENDIENTE-5b3ff97f.md`](PENDIENTE-5b3ff97f.md) y este archivo no la reemplaza.
>
> **Advertencia de método:** estas frases describen el mecanismo tal como se manifestaría
> **en nightshift mismo** (captura, store, retrieval, hook), que es un dominio distinto
> del de la trayectoria `5b3ff97f`. Sólo 2 de las 5 (la 2 y la 4) parafrasean la conjetura
> que tienen enfrente; las otras tres hablan de otro síntoma. Se miden igual, por orden
> expresa, pero el número no es sensibilidad conjetura-por-conjetura.
>
> Las paráfrasis anteriores (v1 y v2, escritas por el agente) están en el historial de
> git: `git show 1761aed:experimentos/retenido/5b3ff97f.md`. Los números citados en
> `doc/HANDOFF.md` §0-bis para el 2026-08-29 (mermaid 2, física 1) se midieron contra
> **esa** versión, no contra ésta.

---

### 1
> Los registros sellados antes del cambio se renderizan bien y sólo los nuevos rompen, lo
> que hace parecer el fallo intermitente.

tu frase:
el prompt no trae la memoria aunque use exactamente las mismas palabras del síntoma

### 2
> El chequeo de salud del store informa todo en verde porque cuenta registros y no campos
> dentro de cada registro.

tu frase:
la base de datos guarda los steps correctamente pero el campo de texto llega vacío

### 3
> Una vista resumida lista el registro sin problema y sólo la vista de detalle explota, con
> el mismo dato de origen.

tu frase:
el agente me dice que no hay contexto previo para este error cuando sí lo hay

### 4
> Otro consumidor del mismo registro devuelve vacío en lugar de fallar, y el hueco pasa
> inadvertido en vez de romper.

tu frase:
el retrieval devuelve un array vacío en la segunda pasada

### 5
> Exportar y reimportar el store fija la pérdida: el campo ya no puede reconstruirse porque
> el canal lateral no viaja con el registro.

tu frase:
los logs muestran que el hook dispara pero el tamaño del contexto no sube

---

Medido por `python3 experimentos/12-sensibilidad.py`. El número que salga se cita SIEMPRE
con la etiqueta de procedencia de arriba.
