# Retenido — trayectoria `5b3ff97f`

> **PROCEDENCIA — SIMULACIÓN AUTORIZADA (v4, 2026-08-29):** las frases de abajo las
> escribió **el agente** (Claude Code) por orden expresa de Matías, simulando cómo se
> quejaría un desarrollador al que le está pasando este bug. **No** es un retenido humano
> bajo el protocolo de [`README.md`](README.md): el mismo autor escribió el retrieval, los
> prompts de consolidación y el instrumento de medición, y además leyó las conjeturas
> antes de parafrasearlas. Cualquier número medido acá es un **techo de autor bajo
> validación simulada**, no transferencia demostrada a una persona. La versión humana
> ciega sigue pendiente en [`PENDIENTE-5b3ff97f.md`](PENDIENTE-5b3ff97f.md) y este archivo
> no la reemplaza.
>
> **Qué cambió en la v4, y por qué.** La v3 (frases dictadas por Matías el 2026-08-29)
> describía el síntoma en el dominio de **nightshift mismo** —hook, retrieval, inyección—
> y la trayectoria `5b3ff97f` es de otro dominio: una etapa parte una respuesta que mezcla
> prosa y objeto, se queda con el objeto, manda la prosa a un archivo lateral, y el
> registro se sella con un campo menos. Medir contra frases de otro bug no mide
> sensibilidad, mide ruido: dio mermaid 1, física 0. La v4 parafrasea **estas** conjeturas,
> en el dominio de registros / store / render que es el suyo.
>
> Las versiones anteriores están en git: `git show 1761aed:experimentos/retenido/5b3ff97f.md`
> (v2, agente) y `git show 3ccb67c:experimentos/retenido/5b3ff97f.md` (v3, Matías).

---

### 1
> Los registros sellados antes del cambio se renderizan bien y sólo los nuevos rompen, lo
> que hace parecer el fallo intermitente.

tu frase:
lo que guardé antes del deploy se ve bien y sólo lo nuevo tira error, parece que falla al azar

### 2
> El chequeo de salud del store informa todo en verde porque cuenta registros y no campos
> dentro de cada registro.

tu frase:
el health check me pasa en verde, cuenta las filas pero no mira si vienen completas

### 3
> Una vista resumida lista el registro sin problema y sólo la vista de detalle explota, con
> el mismo dato de origen.

tu frase:
en el listado aparece sin drama y el detalle del mismo item revienta

### 4
> Otro consumidor del mismo registro devuelve vacío en lugar de fallar, y el hueco pasa
> inadvertido en vez de romper.

tu frase:
el otro endpoint que lee lo mismo me devuelve vacío en vez de fallar y no me di cuenta por días

### 5
> Exportar y reimportar el store fija la pérdida: el campo ya no puede reconstruirse porque
> el canal lateral no viaja con el registro.

tu frase:
hice un dump y lo volví a importar y ese campo ya no se recupera, se perdió en el camino

---

Medido por `python3 experimentos/12-sensibilidad.py`. El número que salga se cita SIEMPRE
con la etiqueta de procedencia de arriba.
